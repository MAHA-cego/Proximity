# TCP port the server listens on. Defaults to 3001 when absent.
PORT=3001

# Allowed CORS origin for the HTTP API. Defaults to * (all origins) when absent.
# In production, set this to the deployed frontend URL (e.g. https://proximity.vercel.app).
ALLOWED_ORIGIN=http://localhost:3000
